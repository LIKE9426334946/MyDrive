{
  "name": "mydrive",
  "version": "1.0.0",
  "private": true,
  "description": "A secure, single-user personal file drive for a private Ubuntu server.",
  "main": "backend/server.js",
  "scripts": {
    "start": "node backend/server.js",
    "test": "node --test",
    "check": "node --check backend/server.js && node --check backend/security.js && node --check backend/storage.js && node --check public/app.js && npm test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^5.1.0",
    "multer": "^2.0.2"
  }
}
